/**
 * Defines the Game object states.
 * @readonly
 * @enum {number}
 */
var GameStates = Object.freeze({
	Initializing: 0,
	Loading: 1,
	Playing: 2,
    Paused: 3,
    GameWin: 4,
    GameOver: 5
});